# Supabase — injected by Vercel Marketplace integration in prod.
# For local dev: run `pnpm dlx supabase start` and copy values from `supabase status`.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Upstash Redis — injected by Vercel Marketplace integration in prod.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Upstash QStash (Phase 08)
QSTASH_URL=
QSTASH_TOKEN=
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=

# Site
NEXT_PUBLIC_SITE_URL=http://localhost:3000
